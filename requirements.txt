fastapi==0.115.8
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-multipart==0.0.9
requests==2.32.3
twilio==9.4.3
aiofiles==23.2.1          # required by FastAPI StaticFiles

# Persistence (baseline relational "Business Brain")
SQLAlchemy==2.0.36

# LLM (Ollama local)
ollama==0.4.5

# Prediction engine
xgboost==2.1.3
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2

# STT
faster-whisper==1.1.0

# TTS — MMS (default, ~350 MB, local Hindi)
transformers>=4.40.0
soundfile>=0.12.1
torch==2.5.1          # shared by STT + TTS + vision

# TTS — Coqui XTTS v2 (optional, high quality, ~4 GB; set JARVIS_TTS_PROVIDER=coqui)
# TTS==0.22.0

# Vision (optional; stub works without it)
# ultralytics==8.3.63
